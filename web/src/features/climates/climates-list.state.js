import { createSlice } from '@reduxjs/toolkit'

const initialState = {
}

export const climatesList = createSlice({
  name: 'climatesList',
  initialState,
  reducers: {
  },
})

export const {
} = climatesList.actions

export default climatesList.reducer